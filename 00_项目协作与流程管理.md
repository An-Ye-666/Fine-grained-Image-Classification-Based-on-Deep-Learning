# 项目协作与流程管理指南

本文件是项目协作方式的起点，用于回答“我们如何一起完成项目”，不代替正式任务书。具体任务要求以《科研项目组本科生考核任务书与操作指南》为准。

## 1. 总原则

1. 仓库是项目记忆，聊天只负责沟通。
2. 先完成必做项，再考虑加分项。
3. 每个阶段先对齐，再实施，再验收。
4. 每次只解决一个明确问题，重大实验一次只改变一个变量。
5. AI 可以辅助写代码，但使用者必须理解关键流程并能够解释。
6. 所有关键决策、实验结果和已知问题都必须写入文件，不能只留在对话中。

## 2. 双方职责

| 角色 | 主要职责 |
| --- | --- |
| 项目负责人 | 明确目标、执行训练、提供真实结果、参与对齐、理解核心代码、完成答辩 |
| 技术搭档 | 拆分任务、设计结构、编写和修改代码、排查问题、检查实验、解释原理 |
| 双方共同 | 阶段对齐、范围确认、结果验收、技术决策、复盘 |

代码可以借助 AI 完成，但项目负责人需要能够回答：输入是什么、输出是什么、为什么这样做、结果是否可靠。

## 3. 事实来源

发生冲突时，按以下优先级判断：

1. 老师给出的任务书；
2. `docs/REQUIREMENTS.md` 中的需求整理；
3. `docs/ALIGNMENT.md` 中已经确认的阶段约定；
4. `docs/DECISIONS.md` 中的技术决策；
5. `docs/STATUS.md` 中的当前进度；
6. 聊天记录。

聊天适合讨论，不适合作为长期记忆。结论必须回写到仓库。

## 4. 阶段划分

| 阶段 | 内容 | 主要产出 |
| --- | --- | --- |
| A0 | 工程骨架与项目控制台 | Git 仓库、目录、需求、架构、状态文件 |
| A1 | 数据与评估协议 | 数据划分、Transform、指标定义 |
| A2 | Baseline | 可复现的 ResNet-18 训练与验证 |
| A3 | 消融实验 | 单变量对比实验与结果表 |
| A4 | 评估与可解释性 | 混淆矩阵、错误案例、Grad-CAM |
| A5 | 工程化与复现 | 模块化代码、CLI、README |
| A6 | 报告与答辩 | PDF 报告、日志、答辩准备 |

阶段状态统一使用：

```text
DRAFT       正在讨论
ALIGNED     已对齐，可以开始
EXECUTING   正在执行
VERIFYING   正在验收
ACCEPTED    已验收
BLOCKED     被阻塞
CHANGED     需要变更约定
```

没有进入 `ALIGNED`，不开始该阶段的关键实现。

## 5. 阶段对齐

每个较大阶段开始前，必须先形成一份对齐说明。最少包含：

```text
阶段目标：
必须完成：
明确不做：
技术方案：
输入与依赖：
输出物：
验收标准：
风险与预案：
需要确认的决定：
```

项目负责人确认后，阶段进入 `ALIGNED`。执行中发现原方案不可行时，进入 `CHANGED`，说明原因和影响，再重新对齐。

## 6. 单次工作循环

每次协作都按以下顺序进行：

```text
检查现状
→ 对齐本次目标
→ 锁定改动范围
→ 修改代码或文档
→ 运行验证
→ 记录证据
→ Git 提交
→ 更新状态
→ 进入下一步
```

每次只推进一个检查点。检查点必须有可观察的结果，例如：

```text
数据类别统计正确
一个 batch 形状为 [32, 3, 224, 224]
模型输出形状为 [32, 37]
一个 epoch 正常完成
验证集指标成功生成
```

## 7. 项目文件

计划逐步建立以下文件：

```text
README.md                 项目说明与复现入口
docs/REQUIREMENTS.md      任务书需求清单
docs/ARCHITECTURE.md      系统架构和数据流
docs/STATUS.md            当前进度、问题和下一步
docs/ALIGNMENT.md         各阶段对齐记录
docs/DECISIONS.md         重要技术决策
docs/EXPERIMENTS.md       实验配置与结果
docs/AI_USAGE_LOG.md      AI 使用与 Bug 复盘
docs/GLOSSARY.md          术语和通俗解释
```

每个文件只承担一种职责，避免把所有信息堆在一个文档中。

## 8. 可视化与证据

项目至少保留三类可视化：

1. 架构图：说明数据和代码如何流动；
2. 状态表：说明当前做到哪里；
3. 实验表：说明每组实验改了什么、结果如何。

每次实验必须记录：

```text
实验编号、Git commit、随机种子、主要配置、
Top-1 Acc、Top-5 Acc、Macro-F1、耗时、
日志路径、结论
```

训练曲线、混淆矩阵、Grad-CAM 和报告文件统一放入 `artifacts/` 下的固定目录。

## 9. 质量门

只有通过当前质量门，才进入下一阶段：

| 质量门 | 检查内容 |
| --- | --- |
| 数据门 | 37 类完整，训练/验证/测试无重叠，比例正确 |
| 形状门 | 图片和模型输出的 Tensor 形状符合预期 |
| 训练门 | 训练和验证完整运行，Loss 不出现异常 |
| 复现门 | 固定配置和随机种子能够重新运行 |
| 实验门 | Baseline 与实验组只改变一个变量 |
| 交付门 | 新环境按 README 可以运行，报告和日志齐全 |

## 10. Git 与变更管理

一个检查点对应一个小提交，提交信息说明做了什么，例如：

```text
feat(data): add stratified data split
feat(model): replace ResNet-18 classifier head
fix(train): use eval mode during validation
docs(status): record baseline result
```

涉及数据划分、模型、损失函数、评价指标、依赖或目录结构的改动，必须先对齐。普通语法修复和小范围文档修改可以直接处理。

## 11. 上下文恢复

每次暂停前，在 `docs/STATUS.md` 中更新：

```text
当前阶段：
已完成：
最后验证结果：
已知问题：
下一步：
最后提交：
```

恢复工作时，先读取状态文件、Git 状态、当前阶段代码和最近实验结果，再继续开发。

## 12. 沟通格式

遇到问题或汇报进度时，尽量提供：

```text
当前阶段：
已经执行：
使用环境：
完整命令：
完整输出或报错：
期望结果：
需要讨论的问题：
```

同一个错误持续 20 到 30 分钟仍无法定位时，停止盲目尝试，转为协作排查。

## 13. 阶段验收与复盘

每个阶段完成后记录：

```text
实际完成：
是否达到验收标准：
产生的文件与证据：
可写入报告的结论：
遗留问题：
下一阶段依赖：
```

阶段没有验收，不进入下一个大阶段。

## 14. 当前起点

当前尚未开始 A0。下一步是在双方确认 A0 范围后，建立正式工程骨架和项目控制台。

本文件生效后，除非双方明确修改，后续协作均按本流程执行。
