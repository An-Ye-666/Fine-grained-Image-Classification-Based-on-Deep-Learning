# 技术决策记录

本文件记录会影响工程结构、实验结论或交付方式的决策。小范围代码修复不在这里记录。

## D001：项目根目录就是当前仓库

状态：ACCEPTED

决策：项目直接使用当前 `AAA科研项目1号` 目录作为仓库根目录，不再嵌套一层项目文件夹。

原因：减少路径层级，便于 Git、README 和后续实验命令保持一致。

## D002：仓库是长期上下文

状态：ACCEPTED

决策：关键目标、范围、进度、实验结果和决策必须写入仓库文件。

原因：避免对话上下文丢失后无法准确恢复项目状态。

## D003：A0 不编写算法实现

状态：ACCEPTED

决策：A0 只建立包边界和项目控制文档，不写数据集、模型和训练代码。

原因：先固定架构与验收规则，再实现细节，减少返工。

## D004：当前结构保持环境无关

状态：ACCEPTED

决策：A0 不绑定 Colab、Kaggle 或本地 GPU，也不提前固定依赖版本。

原因：训练环境尚未确定，过早耦合会增加迁移成本。

## D005：Python 解释器可用，训练环境仍需 A1 确认

状态：ACCEPTED

决策：使用 `C:\Users\19603\AppData\Local\Programs\Python\Python314\python.exe` 验证 Python 3.14.4 和 pip 26.0.1；A0 不安装训练依赖。

原因：解释器可用不等于 PyTorch 等训练依赖已经兼容。实际训练环境及依赖版本在 A1 确认。

## 待决事项

| ID | 待决内容 | 对齐阶段 |
| --- | --- | --- |
| Q001 | 使用哪个训练环境 | A2，已由 D010 更新为本地 CPU |
| Q002 | 官方划分与 70/15/15 重划分的关系 | A1，已确认 |
| Q003 | 第一组和第二组消融实验 | 第一组已完成，第二组暂不执行 |
| Q004 | 模型选择使用验证 Accuracy 还是 Macro-F1 | 已选择验证 Top-1 |
| Q005 | 如何在 Colab 上处理本地与云端依赖版本 | 已取消，改用本地 CPU |

## D006：使用官方清单后重新分层

状态：ACCEPTED

决策：合并官方 `trainval.txt` 和 `test.txt`，得到 7,349 个有效样本，再按 `seed=42` 生成 70/15/15 分层划分。

原因：任务书要求约 7,349 张图片按 70/15/15 划分，同时保证每个类别分布一致。图片目录中存在但不在官方清单中的图片不参与实验。

## D007：测试集在实验完成前保持锁定

状态：ACCEPTED

决策：训练和模型选择只使用训练集与验证集；所有配置冻结后才在测试集上做最终评估。

原因：避免反复查看测试集造成信息泄漏，使最终指标失去独立评价意义。

## D008：A1 本地验证使用 CPU

状态：ACCEPTED

决策：本地 Python 3.14 与 PyTorch 2.14 CPU 版本用于数据检查和轻量验证；正式训练设备在 A2 阶段确定。

原因：本地 CUDA 不可用。A1 不需要 GPU，因此不阻塞阶段验收。

## D009：A2 使用 Colab Tesla T4

状态：SUPERSEDED

决策：A2 的 GPU 训练目标环境是 Colab Tesla T4。当前已在远程 notebook 中确认 Linux、`/content`、`torch 2.11.0+cu128`、`CUDA=True` 和 `Tesla T4`。

原因：本地只有 CPU PyTorch，不适合作为主要训练环境。项目代码和数据在 Colab 上的部署、依赖版本匹配和跨环境 A1 检查推迟到 A2。

## D010：最终模型与训练环境

状态：ACCEPTED

决策：A2 Baseline 和 A3 消融实验最终都在本地 CPU 完成。最终模型使用
Label Smoothing 0.1 实验的第 9 轮 checkpoint，并在 A4 中只解锁测试集一次。

原因：Label Smoothing 模型在验证集上取得更高的 Top-1 和 Macro-F1。测试集此前
未被使用，因此可以作为最终独立评估集。由于提升来自单次实验，报告结论只限定在
当前数据划分、随机种子和训练配置下。
